# Notes

- claude keeps putting domain logic in adapter logic, ensure adapter logic is purely how to interact with state storage or a 3rd party API
- We need a storage adapter then to move all agent and memory logic to the domain folder 